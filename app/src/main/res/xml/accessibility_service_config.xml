<?xml version="1.0" encoding="utf-8"?>
<!--
    Konfigurasi AccessibilityService untuk Puzzle Bot.

    accessibilityEventTypes="typeAllMask"
        → Dengarkan semua event (agar bisa deteksi kapan puzzle muncul)

    accessibilityFlags="flagRequestFilterKeyEvents|flagRetrieveInteractiveWindows"
        → Izin untuk melihat window aktif

    canPerformGestures="true"
        → INI YANG PALING PENTING: izin inject gesture/swipe tanpa root

    canRetrieveWindowContent="true"
        → Izin baca konten window
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRequestFilterKeyEvents|flagRetrieveInteractiveWindows"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_description"
    android:notificationTimeout="100"
    android:settingsActivity=".MainActivity" />
